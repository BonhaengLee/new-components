import styles from "./Footer.module.scss";

/* NavBar와 관련된 margin-bottom 설정을 위한 임시 코드 */
const index = (): JSX.Element => {
  return <footer className={styles.ly_footer}>footer</footer>;
};

export default index;
